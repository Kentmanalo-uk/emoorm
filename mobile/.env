# Leave unset during development: the app uses the IP of the computer running Expo (port 3000).
# Set it only to point at a specific backend, e.g. EXPO_PUBLIC_API_BASE_URL=http://10.0.8.115:3000/api
# EXPO_PUBLIC_API_BASE_URL=
